import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  
    <App />
  
  // </React.StrictMode> 모던 자바스크립트를 엄격하게 검증하고 실행하는 모드(콘솔창에 두번씩 찍히게 된다)

);
